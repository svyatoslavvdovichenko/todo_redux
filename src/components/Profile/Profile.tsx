import { FC } from 'react';
import { Helmet } from "react-helmet";

const Profile: FC = () => (
  <>
    <Helmet>
      <title>Profile</title>
    </Helmet>
    
    Profile
  </>
);


export default Profile;
